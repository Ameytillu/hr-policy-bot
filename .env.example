# Offline mode is the safe default and does not require an API key.
OPENAI_API_KEY=
EMBEDDINGS_PROVIDER=st
EMBEDDINGS_MODEL=all-MiniLM-L6-v2
USE_LLM=false
USE_DENSE=false
TOP_K=6
MIN_RELEVANCE_SCORE=0.05
GEN_MODEL=gpt-4o-mini
